# Remove keyboard shortcut aliases / functions
# unalias deps
unset -f deps cd freeze size sizes setup_venv freeze_venv reinstall_venv force_reinstall_venv activate_venv deactivate_venv reset_venv reinstall_python pip

echo "Removed deps, cd, freeze, size, sizes, setup_venv, freeze_venv, reinstall_venv, force_reinstall_venv, activate_venv, deactivate_venv, reset_venv, reinstall_python functions, pip functions"
