# Remove keyboard shortcut aliases / functions
# unalias deps
unset -f deps deps_tree cd freeze size sizes setup_venv freeze_venv reinstall_venv force_reinstall_venv activate_venv deactivate_venv reset_venv reinstall_python pip large_folders last_updates

echo "Removed deps, deps_tree, cd, freeze, size, sizes, setup_venv, freeze_venv, reinstall_venv, force_reinstall_venv, activate_venv, deactivate_venv, reset_venv, reinstall_python functions, pip, large_folders, last_updates functions"
