# Remove keyboard shortcut aliases / functions
# unalias deps
unset -f deps freeze size sizes setup_venv freeze_venv reinstall_venv force_reinstall_venv activate_venv deactivate_venv reinstall_python pip

echo "Removed deps, freeze, size, sizes, setup_venv, freeze_venv, reinstall_venv, force_reinstall_venv, activate_venv, deactivate_venv, reinstall_python functions, pip functions"
